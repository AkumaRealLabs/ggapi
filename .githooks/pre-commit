#!/bin/sh

branch=$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)

if [ "$branch" = "main" ]; then
  echo "ggapi guard: routine commits on main are forbidden; create a topic branch and use a PR." >&2
  exit 1
fi

exit 0
